# 固件改造路径

## 已确认

- 业务控制走 `FFF0/FFF3`，但充电四格动画不受一次性关灯帧控制。
- 从设备二维码官方下载并反编译的 Colorful Lights 1.1.1 APK（SHA-256 `6e7071e9e84cc64a41d3e4901e0d7e8be6312fe0ccb69432be0dcc41ccc540f3`）不包含灯具固件文件，也没有关闭充电/电量显示的命令。
- 设备暴露 `5833FF01/FF02/FF03` OTA 服务；这一 UUID 族用于 HopeRF CMT4522/CMT4552 等 BLE SoC/模块的 OTA。
- OTA 特征只允许 `write/notify`，不能从 Mac 反向读取现有固件镜像。
- HopeRF 官方为 CMT4522 提供 SDK、烧录工具、512KB/2MB Flash 和 OTA 支持。

## 目标固件行为

1. 默认 `charge_indicator_enabled = false`，插电和充电状态变化都不绘制 LED。
2. 电源键短按不再自动显示电量，除非进入临时预览状态。
3. 新增 BLE 命令：隐藏、显示 10 秒、恢复原厂显示逻辑。
4. 配置掉电保存；刷写前保留完整原厂固件备份，可一键回刷。

## 下一步需要的硬件信息

BLE OTA 无法读取原固件，因此先打开外壳并拍摄 PCB 正反面、主控芯片丝印和测试焊盘。确认芯片/封装和 SWD/UART/SPI 焊盘后：

1. 用对应烧录器只读导出完整 Flash。
2. 验证备份可重复读取且哈希一致。
3. 在原固件二进制上定位充电 GPIO/ADC 判断和四格渲染路径，或基于官方 SDK 重建兼容固件。
4. 先在备用镜像测试，再通过有线烧录或设备 OTA 更新。

不要在没有原厂备份和确切芯片型号的情况下向 `5833FF02` 写入固件。
